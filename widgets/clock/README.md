# Clock

A simple clock showing the current local time and date, updating every second. A
producer-less, pure-client example widget — the smallest possible CW widget and a good
starting template. The ⚙ "Show seconds" option is stored per-instance via the Hub config API.
