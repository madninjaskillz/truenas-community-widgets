{
  "id": "http-endpoint",
  "name": "HTTP Endpoint",
  "version": "1.0.0",
  "author": "CW",
  "description": "Monitor one endpoint: status code + a live response-time graph.",
  "client": "client.js",
  "supportedSizes": ["quarter", "half", "full"],
  "config": {
    "url": { "type": "string", "label": "URL", "default": "" },
    "interval": { "type": "number", "label": "Refresh seconds", "default": 15 }
  }
}
